func main #For C code, this is REQUIRED to run.
	x = 3
	y = 1
	z = 2
	a = x * (y+z) #We need to simplify for expressions too.

	t = 3 * (1+(1*2)) #We need to simplify this before translating it to C. UNLESS it is a variable expression?
	b = 1 * 2 - 3 + 4 / 5 + 6 * 7
	f = 2.9 + 3.2
	d = "banana" + "rama" + "lolol" + " yes" + " i probably" + " did it" + " with odds as well"
	#c + a - b + c #is this even valid C?

func add num1 num2
fnfo int int
fret int
	return num1 + num2